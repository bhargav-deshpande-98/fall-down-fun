import { FallDownGame } from '@/components/game/FallDownGame';

const Index = () => {
  return (
    <div className="fixed inset-0 w-screen h-screen overflow-hidden bg-background">
      <FallDownGame />
    </div>
  );
};

export default Index;
